export type CardTemplate = {
  id: string;
  title: string;
  maxPoints: number;
  cardColor: string;
  accentColor: string;
  textColor: string;
  isActive: boolean;
  businessId: string;
  createdAt: string;
  updatedAt: string;
};
