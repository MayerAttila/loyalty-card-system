export type CardTemplate = {
  id: string;
  title: string;
  maxPoints: number;
  cardColor: string;
  accentColor: string;
  textColor: string;
  businessId: string;
  createdAt: string;
  updatedAt: string;
};
